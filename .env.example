# Anthropic API key — get yours at https://console.anthropic.com/
# This key is ONLY used by the Vite dev server proxy.
# It is never bundled into the browser/client code.
ANTHROPIC_API_KEY=sk-ant-your-key-here
